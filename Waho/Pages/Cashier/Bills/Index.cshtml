@page
@model Waho.Pages.Cashier.Bills.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Bill[0].Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Bill[0].Customer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Bill[0].UserNameNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Bill) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Customer.CustomerId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserNameNavigation.UserName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.BillId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.BillId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.BillId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
